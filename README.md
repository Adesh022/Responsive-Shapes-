# Responsive-Shapes-
A simple HTML and CSS project that displays three different shapes — square, diamond, and circle — with centered numbers and interactive hover effects. Each shape changes its background color when hovered, demonstrating the use of CSS Flexbox, transforms, border-radius, gradients, and hover effects.
